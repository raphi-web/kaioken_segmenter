# Core ML
torch>=2.0
segmentation-models-pytorch>=0.5
timm>=1.0
albumentations>=2.0
numpy>=1.26,<2

# Geospatial I/O
rasterio>=1.4,<1.5

# Desktop bridge / imaging
pywebview>=6
pillow>=10

# SAM2 "snap-to-edge" click assist — runtime (encoder/decoder run in ONNX).
# torchvision + opencv are used only for preprocessing/mask post-processing.
onnxruntime>=1.20
torchvision>=0.15
opencv-python-headless>=4.8

# SAM2 ONNX export only (backend/export_sam_onnx.py); not needed to run the app:
#   onnx  hydra-core  omegaconf  iopath   (plus the sam2/Efficient-SAM2 repo + *.pt)
#
# "Export Executable" only: the standalone predictor (standalone/) is packaged
# with PyInstaller; build-time dep, not imported by the app:  pyinstaller

# Linux renderer note: pywebview uses GTK/WebKit2 (PyGObject + gir1.2-webkit2-4.1)
# if available; otherwise install the Qt extra:  pip install pywebview[qt]
